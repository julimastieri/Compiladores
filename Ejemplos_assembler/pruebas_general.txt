int a, b;
int d[0,2,3];
ulong c,w;
ulong z[2,2,3];

begin

	w:=to_ulong(2);

	#c:=to_ulong(2)/z[0]; #REG-VAR

	c:=to_ulong(2)/to_ulong(2);

	if (c == 1) print({Anda Bien RM-RM}); else print({Anda Mal}); end_if;


end