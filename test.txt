# Asignaciones
int a;
int b;
int c [1,2,3];
ulong d;

begin
	a := 8;
	b := a;
	b := 0;

	c[1] := 4;                  
	c[b] := a;                 

	a := 2 + a * b;             
	d := to_ulong(a);           
	d := to_ulong(2+a)/to_ulong(c[1]);    

	b := c[0];					
end